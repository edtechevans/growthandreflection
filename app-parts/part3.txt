  $('#recordAudio').addEventListener('click',async()=>{const btn=$('#recordAudio'),status=$('#audioStatus');if(recorder&&recorder.state==='recording'){recorder.stop();btn.classList.remove('is-recording');btn.querySelector('strong').textContent='Start recording';status.textContent='Processing recording…';return}try{const stream=await navigator.mediaDevices.getUserMedia({audio:true});audioChunks=[];recorder=new MediaRecorder(stream);recorder.ondataavailable=e=>{if(e.data.size)audioChunks.push(e.data)};recorder.onstop=()=>{const blob=new Blob(audioChunks,{type:recorder.mimeType||'audio/webm'});if(audioUrl)URL.revokeObjectURL(audioUrl);audioUrl=URL.createObjectURL(blob);const audio=$('#audioPlayback');audio.src=audioUrl;audio.hidden=false;status.textContent='Recording ready for reflection.';stream.getTracks().forEach(t=>t.stop())};recorder.start();btn.classList.add('is-recording');btn.querySelector('strong').textContent='Stop recording';status.textContent='Recording… tap again when finished.'}catch(err){status.textContent='Microphone access is unavailable here. Upload audio instead.';showToast('Microphone unavailable — use upload instead.')}});

  $('#analyseEvidence').addEventListener('click',()=>{if(!activeMode)return;if(activeMode==='video'&&!$('#videoInput').files[0])return showToast('Choose or record a video first.');if(activeMode==='audio'&&!audioUrl)return showToast('Record or upload a voice note first.');if(activeMode==='file'&&!selectedFile)return showToast('Choose a file first.');if(activeMode==='write'&&!$('#writtenEvidence').value.trim())return showToast('Write or paste something first.');if(activeMode==='observation'&&!$('#observationNotes').value.trim())return showToast('Paste or upload classroom observation notes first.');captureWorkspace.hidden=true;analysisScreen.hidden=false;resultScreen.hidden=true;resultScreen.classList.remove('observation-mode');const analysisTitle=$('#analysisScreen h2');const isObservation=activeMode==='observation';analysisTitle.textContent=isObservation?'Connecting evidence to one TLF indicator at a time.':'Looking for evidence before advice.';const messages=isObservation?['Reading the observation notes…','Looking for specific, observable evidence…','Matching each point to one TLF indicator only…','Preparing two commendations and one wondering…']:['Reading what you shared…','Looking for observable evidence…','Connecting only the relevant AISG sources…','Preparing one useful question and next move…'];let i=0;$('#analysisMessage').textContent=messages[0];const timer=setInterval(()=>{i++;if(i<messages.length)$('#analysisMessage').textContent=messages[i];else{clearInterval(timer);renderResult(activeMode)}},540)});

  function noteSnippets(text){const clean=String(text||'').replace(/\s+/g,' ').trim();const sentences=clean.split(/(?<=[.!?])\s+/).map(s=>s.trim()).filter(Boolean);return sentences.length?sentences:[clean]}
  function clip(text,max=220){const value=String(text||'').trim();return value.length>max?`${value.slice(0,max-1).trim()}…`:value}

  function renderObservationResult(){
    $('#view-reflect').classList.add('showing-result');analysisScreen.hidden=true;resultScreen.hidden=false;resultScreen.classList.add('observation-mode');
    const observationResult=$('#observationResult');observationResult.hidden=false;
    $('.result-header .eyebrow').textContent='Classroom Observation Notes';$('#resultTitle').textContent='Two commendations. One wondering.';
    const raw=$('#observationNotes').value.trim();const snippets=noteSnippets(raw);const pick=i=>clip(snippets[i]||snippets[0]||raw,190);
    $('#observationEvidenceExcerpt').textContent=clip(raw,520);
    $('#observationNoteOne').textContent=`“${pick(0)}”`;
    $('#observationNoteTwo').textContent=`“${pick(1)}”`;
    $('#observationNoteThree').textContent=`“${pick(2)}”`;
    window.scrollTo({top:0,behavior:'smooth'});
  }

  function renderResult(mode){
    if(mode==='observation'){renderObservationResult();return}
    $('#view-reflect').classList.add('showing-result');analysisScreen.hidden=true;resultScreen.hidden=false;resultScreen.classList.remove('observation-mode');const observationResult=$('#observationResult');if(observationResult)observationResult.hidden=true;$('.result-header .eyebrow').textContent='Reflection';
    const r=demo[mode]||demo.write;$('#resultTitle').textContent=r.title;$('#noticeTitle').textContent=r.noticeTitle;$('#noticeBody').textContent=r.noticeBody;$('#evidenceBox').textContent=r.evidence;$('#connectionTitle').textContent=r.connectionTitle;$('#connectionBody').textContent=r.connectionBody;$('#frameworkChips').innerHTML=r.chips.map(x=>`<span>${escapeHtml(x)}</span>`).join('');$('#groundingCopy').textContent=r.grounding;$('#wonderingResult').textContent=r.wondering;$('#nextTitle').textContent=r.nextTitle;$('#nextBody').textContent=r.nextBody;$('#watchBody').textContent=r.watch;$('#goalConnectionCopy').textContent=r.goal;const label={video:'Classroom video',audio:'Voice reflection',file:'Unit / learning resource',write:'Written reflection'}[mode];$('#evidenceLabel').textContent=label;const detail=mode==='video'&&$('#videoInput').files[0]?$('#videoInput').files[0].name:mode==='file'&&selectedFile?selectedFile.name:mode==='audio'&&audioUrl?'Recorded or uploaded audio':'Demo evidence';$('#evidenceDetail').textContent=detail;$('#coachMessages').innerHTML='<div class="coach-message ai">I’m grounding this reflection in your evidence and the AISG sources shown above. What would you like to think through?</div>';window.scrollTo({top:0,behavior:'smooth'})}

  $('#saveGrowth').addEventListener('click',()=>showToast('Saved to My Growth.'));$('#connectGoal').addEventListener('click',()=>showToast('Connected to My Goal.'));$('#adaptThis').addEventListener('click',()=>{$('#coachInput').value='Help me adapt this learning experience based on the reflection, while keeping the learning goal and appropriate challenge clear.';sendCoach();showToast('Opened in the reflection coach.')});$('#dismissSuggestion').addEventListener('click',()=>showToast('Suggestion dismissed. Your evidence remains.'));$('#challengeInterpretation').addEventListener('click',()=>{$('#coachInput').value='I disagree with this interpretation. Explain your reasoning and offer another plausible interpretation of the same evidence.';sendCoach();$('#coachInput').scrollIntoView({behavior:'smooth',block:'center'})});$('#shareResult').addEventListener('click',async()=>{let text;if(activeMode==='observation'){text=`AISG Reflect · Classroom Observation Notes\n\nCommendation 1 — TLF 6.1 Collaboration\n${$('.commendation-card:nth-of-type(1) h2').textContent}\n\nCommendation 2 — TLF 1.4 Personalisation\n${$('.commendation-card:nth-of-type(2) h2').textContent}\n\nWondering — TLF 2.2 Agency\n${$('.wondering-card h2').textContent}`;}else{text=`AISG Reflect\n\n${$('#noticeTitle').textContent}\n${$('#noticeBody').textContent}\n\nQuestion: ${$('#wonderingResult').textContent}\n\nNext move: ${$('#nextTitle').textContent} — ${$('#nextBody').textContent}`;}try{await navigator.clipboard.writeText(text);showToast('Reflection summary copied.')}catch{showToast('Copy is not available in this browser.')}});

  $('#coachSend').addEventListener('click',sendCoach);$('#coachInput').addEventListener('keydown',e=>{if(e.key==='Enter')sendCoach()});$$('.coach-suggestions [data-coach]').forEach(b=>b.addEventListener('click',()=>{$('#coachInput').value=b.dataset.coach;sendCoach()}));
  function sendCoach(){const input=$('#coachInput');const q=input.value.trim();if(!q)return;const box=$('#coachMessages');box.insertAdjacentHTML('beforeend',`<div class="coach-message user">${escapeHtml(q)}</div>`);input.value='';const lower=q.toLowerCase();let a='One alternative is to change the condition around the learning rather than add a new activity: clarify what students own, keep the learning goal fixed, and collect evidence of how they respond.';if(lower.includes('tlf'))a='The TLF connection is based on the learner experience described in the evidence, not on a keyword match. I would only keep the connection if the observed student experience genuinely aligns with that facet.';if(lower.includes('access')||lower.includes('challenge'))a='Keep the cognitive goal common while widening the pathways into the learning: examples, language supports, structured choice, models or flexible ways to demonstrate understanding can improve access without reducing challenge.';if(lower.includes('disagree')||lower.includes('challenge the ai'))a='That is a reasonable challenge. The interpretation is only one possibility. The evidence itself is narrower than the conclusion, so we should keep the claim tentative and look for another source of evidence before treating it as a pattern.';setTimeout(()=>{box.insertAdjacentHTML('beforeend',`<div class="coach-message ai">${escapeHtml(a)}</div>`);box.lastElementChild.scrollIntoView({behavior:'smooth',block:'nearest'})},260)}

  function formatBytes(n){if(!n)return'0 B';const u=['B','KB','MB','GB'];const i=Math.min(Math.floor(Math.log(n)/Math.log(1024)),3);return`${(n/Math.pow(1024,i)).toFixed(i?1:0)} ${u[i]}`}
  function escapeHtml(s){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
  function showToast(msg){toast.textContent=msg;toast.classList.add('show');clearTimeout(showToast.t);showToast.t=setTimeout(()=>toast.classList.remove('show'),1900)}
})();
